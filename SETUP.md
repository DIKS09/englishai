# 🚀 Пошаговая инструкция по настройке

## Шаг 1: Настройка MongoDB Atlas

1. Перейдите на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas/register)
2. Зарегистрируйтесь (можно через Google)
3. Создайте новую организацию и проект
4. Нажмите **"Build a Database"**
5. Выберите **FREE tier** (M0 Sandbox)
6. Выберите регион (например, AWS - Frankfurt)
7. Нажмите **"Create"**

### Настройка доступа:

8. **Database Access**: 
   - Нажмите "Add New Database User"
   - Username: `englishAI` (или любой другой)
   - Password: создайте надежный пароль (сохраните его!)
   - Database User Privileges: **Read and write to any database**
   - Нажмите "Add User"

9. **Network Access**:
   - Нажмите "Add IP Address"
   - Нажмите "Allow Access from Anywhere" (0.0.0.0/0)
   - Нажмите "Confirm"

10. **Получение строки подключения**:
    - Вернитесь в "Database"
    - Нажмите кнопку **"Connect"**
    - Выберите **"Connect your application"**
    - Скопируйте строку подключения
    - Замените `<password>` на ваш реальный пароль
    - Замените `<dbname>` на `englishAI`

Пример итоговой строки:
```
mongodb+srv://englishAI:myPassword123@cluster0.abc123.mongodb.net/englishAI?retryWrites=true&w=majority
```

## Шаг 2: Настройка OpenAI API

1. Перейдите на [OpenAI Platform](https://platform.openai.com/signup)
2. Зарегистрируйтесь (можно через Google)
3. Пополните баланс (минимум $5)
   - Перейдите в **Billing** → **Add payment method**
4. Перейдите в **API Keys**
5. Нажмите **"Create new secret key"**
6. Дайте ключу имя: `EnglishAI`
7. **ВАЖНО**: Скопируйте ключ сразу! Он больше не отобразится!

Пример ключа:
```
sk-proj-abc123xyz789...
```

## Шаг 3: Создание файла .env

В корневой папке проекта создайте файл `.env` со следующим содержимым:

```env
# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://englishAI:ВАШ_ПАРОЛЬ@cluster0.abc123.mongodb.net/englishAI?retryWrites=true&w=majority

# OpenAI API Key
OPENAI_API_KEY=sk-proj-ВАШ_КЛЮЧ_OPENAI

# Server Configuration
PORT=5000
NODE_ENV=development
```

**Замените**:
- `ВАШ_ПАРОЛЬ` - на пароль от MongoDB Atlas
- `ВАШ_КЛЮЧ_OPENAI` - на ваш API ключ от OpenAI

## Шаг 4: Запуск приложения

### Windows (PowerShell):

```powershell
# Запуск backend (терминал 1)
npm run dev

# Запуск frontend (терминал 2)
cd frontend
npm start
```

### Или запустите всё одной командой:

```powershell
npm run dev:all
```

## Шаг 5: Открытие приложения

1. Frontend откроется автоматически в браузере: `http://localhost:3000`
2. Backend работает на: `http://localhost:5000`

## ✅ Проверка работы

### Проверка Backend:
Откройте в браузере: `http://localhost:5000/api/health`

Должен вернуться:
```json
{"status":"OK","message":"Server is running"}
```

### Проверка Frontend:
Откройте: `http://localhost:3000`

Вы должны увидеть красивую главную страницу с тремя карточками функций.

## 🐛 Решение проблем

### Проблема: "MongoServerError: bad auth"
**Решение**: Проверьте правильность пароля в MONGODB_URI

### Проблема: "OpenAI API error 401"
**Решение**: Проверьте правильность OPENAI_API_KEY

### Проблема: "OpenAI API error 429"
**Решение**: 
- Проверьте баланс на OpenAI Platform
- Возможно превышен лимит запросов

### Проблема: Порт 3000 или 5000 занят
**Решение**: 
- Закройте другие приложения на этих портах
- Или измените PORT в .env файле

### Проблема: "Cannot find module"
**Решение**: 
```bash
npm install
cd frontend && npm install
```

## 💡 Советы

1. **Не коммитьте файл .env** - он уже в .gitignore
2. **Следите за балансом OpenAI** - каждый запрос стоит денег
3. **MongoDB Atlas Free Tier** - имеет лимит 512 МБ данных
4. **Используйте разные ключи** для dev и production

## 📞 Поддержка

Если ничего не помогло:
1. Проверьте логи в терминале
2. Откройте консоль браузера (F12)
3. Убедитесь, что все зависимости установлены
4. Перезапустите серверы

---

**Готово! Теперь вы можете изучать английский с помощью ИИ! 🎉**

