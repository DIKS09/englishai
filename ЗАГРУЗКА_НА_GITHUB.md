# 🚀 Загрузка проекта на GitHub

## 📋 Что это делает

Этот скрипт автоматически:
1. ✅ Инициализирует Git репозиторий
2. ✅ Добавит все файлы проекта
3. ✅ Создаст первый коммит
4. ✅ Подключится к вашему GitHub репозиторию
5. ✅ Загрузит весь код на GitHub

---

## ⚡ БЫСТРЫЙ СПОСОБ (Автоматический)

### Шаг 1: Запустите скрипт

Дважды кликните на файл: **`setup-github.bat`**

Или откройте PowerShell/CMD в папке проекта и выполните:
```cmd
setup-github.bat
```

### Шаг 2: Следуйте инструкциям

Скрипт попросит:
1. Ваше имя для Git
2. Ваш email
3. Создать репозиторий на GitHub (скрипт даст паузу)
4. Ваш GitHub username
5. Логин и пароль для загрузки

**Готово!** Код будет на GitHub!

---

## 📝 РУЧНОЙ СПОСОБ (Если скрипт не работает)

### Шаг 1: Инициализируйте Git

Откройте PowerShell в папке проекта и выполните:

```powershell
# Удалите старые .git папки (если есть)
Remove-Item -Recurse -Force .git -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force frontend\.git -ErrorAction SilentlyContinue

# Инициализируйте Git
git init

# Настройте Git (замените на свои данные)
git config user.name "Ваше Имя"
git config user.email "your.email@example.com"
```

---

### Шаг 2: Добавьте файлы и создайте коммит

```powershell
# Добавьте все файлы
git add .

# Создайте коммит
git commit -m "Initial commit: EnglishAI project"
```

---

### Шаг 3: Создайте репозиторий на GitHub

1. Откройте: **https://github.com/new**
2. Repository name: `englishai` (или другое имя)
3. Description: `AI-powered English learning platform`
4. Выберите: **Public** (или Private)
5. **НЕ** ставьте галочки на:
   - ❌ Add a README file
   - ❌ Add .gitignore
   - ❌ Choose a license
6. Нажмите **"Create repository"**

---

### Шаг 4: Подключите репозиторий и загрузите код

**Замените `YOUR_USERNAME` на ваш username GitHub:**

```powershell
# Добавьте remote
git remote add origin https://github.com/YOUR_USERNAME/englishai.git

# Переименуйте ветку в main
git branch -M main

# Загрузите код на GitHub
git push -u origin main
```

**Пример:**
Если ваш username: `alice123`
```powershell
git remote add origin https://github.com/alice123/englishai.git
git branch -M main
git push -u origin main
```

---

### Шаг 5: Введите логин и пароль

Git попросит:
- **Username:** ваш GitHub username
- **Password:** ваш GitHub password или Personal Access Token

**Важно:** Если пароль не работает, нужен Personal Access Token:

1. Откройте: https://github.com/settings/tokens
2. Generate new token (classic)
3. Выберите срок действия и права (repo)
4. Скопируйте токен
5. Используйте его вместо пароля

---

## ✅ Проверка

После загрузки:

1. Откройте: `https://github.com/YOUR_USERNAME/englishai`
2. Убедитесь что все файлы загружены
3. Должны увидеть:
   - 📂 backend/
   - 📂 frontend/
   - 📄 package.json
   - 📄 README.md
   - И все остальные файлы

---

## 🎉 Готово!

Код загружен на GitHub!

**Следующий шаг:** Откройте файл **`ДЕПЛОЙ_НА_RENDER.md`** и переходите к **ЧАСТИ 5** (деплой на Render)

---

## 🆘 Проблемы?

### ❌ Git не найден

```
'git' is not recognized...
```

**Решение:** Установите Git
1. Скачайте: https://git-scm.com/download/win
2. Установите (все по умолчанию)
3. Перезапустите PowerShell
4. Попробуйте снова

---

### ❌ Ошибка аутентификации

```
Authentication failed
```

**Решение:** Используйте Personal Access Token:
1. https://github.com/settings/tokens
2. Generate new token (classic)
3. Права: `repo` (все галочки)
4. Скопируйте токен
5. Используйте токен вместо пароля

---

### ❌ Ошибка remote already exists

```
remote origin already exists
```

**Решение:**
```powershell
git remote remove origin
git remote add origin https://github.com/YOUR_USERNAME/englishai.git
git push -u origin main
```

---

### ❌ Ошибка "Repository not found"

**Причины:**
1. Неправильный username в URL
2. Репозиторий не был создан на GitHub
3. Репозиторий private и нет прав

**Решение:**
1. Проверьте URL: `https://github.com/YOUR_USERNAME/englishai`
2. Убедитесь что репозиторий создан
3. Проверьте username

---

## 📞 Альтернативный способ - GitHub Desktop

Если командная строка не работает, используйте GitHub Desktop:

1. Скачайте: https://desktop.github.com/
2. Установите и войдите в аккаунт
3. File → Add Local Repository
4. Выберите папку проекта
5. Publish repository
6. Готово!

---

## 🔄 Как обновить код на GitHub в будущем

После изменений в коде:

```powershell
git add .
git commit -m "Описание изменений"
git push origin main
```

Render автоматически подхватит изменения и задеплоит!

---

## 📊 Структура Git

После загрузки:

```
GitHub Repository (https://github.com/username/englishai)
├── .gitignore          ✓ (скрывает node_modules и .env)
├── backend/            ✓
├── frontend/           ✓
├── package.json        ✓
├── README.md           ✓
└── все документы       ✓
```

---

**Готово! Переходите к деплою на Render! 🚀**

