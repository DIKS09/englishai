# 🧠 EnglishAI - Сайт-помощник для изучения английского с нейросетью

Полнофункциональный сайт-тренажер для изучения английского языка с использованием искусственного интеллекта (OpenAI GPT), React и MongoDB Atlas.

## ✨ Функции

### 1. 📝 Генератор тем для эссе
Введите ключевое слово (например, "technology") и получите 5 интересных и актуальных тем для написания эссе.

### 2. 💬 Создатель диалогов
Выберите тему разговора (например, "в ресторане") и уровень сложности (легкий/средний/сложный), и ИИ создаст реалистичный диалог для практики.

### 3. ✏️ Упражнение "Заполни пропуск"
Укажите грамматическую тему (например, "Present Perfect") и получите упражнения с пропущенными словами для тренировки.

## 🛠 Технологии

### Frontend
- ⚛️ React 18
- 🎨 Custom CSS (красивый градиентный дизайн)
- 🔥 React Icons
- 📡 Axios для API запросов

### Backend
- 🟢 Node.js + Express
- 🍃 MongoDB Atlas (облачная база данных)
- 🤖 OpenAI GPT-3.5 Turbo
- 🔒 CORS и Body Parser

## 📦 Установка

### Требования
- Node.js (версия 14 или выше)
- npm или yarn
- Аккаунт MongoDB Atlas
- OpenAI API ключ

### Шаг 1: Клонирование и установка зависимостей

```bash
# Установка зависимостей для backend
npm install

# Установка зависимостей для frontend
cd frontend
npm install
cd ..
```

### Шаг 2: Настройка переменных окружения

Создайте файл `.env` в корневой папке проекта:

```env
# MongoDB Atlas Connection
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/englishAI?retryWrites=true&w=majority

# OpenAI API Key
OPENAI_API_KEY=sk-ваш-ключ-openai

# Server Configuration
PORT=5000
NODE_ENV=development
```

#### Как получить MongoDB URI:
1. Зарегистрируйтесь на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте бесплатный кластер (Free Tier)
3. Нажмите "Connect" → "Connect your application"
4. Скопируйте строку подключения и замените `<password>` на ваш пароль

#### Как получить OpenAI API ключ:
1. Зарегистрируйтесь на [OpenAI Platform](https://platform.openai.com/)
2. Перейдите в раздел API Keys
3. Создайте новый ключ
4. Скопируйте ключ (он показывается только один раз!)

### Шаг 3: Запуск приложения

#### Вариант 1: Запуск backend и frontend отдельно

```bash
# Терминал 1 - Backend
npm run dev

# Терминал 2 - Frontend
cd frontend
npm start
```

#### Вариант 2: Запуск всего сразу

```bash
npm run dev:all
```

Приложение откроется по адресу: `http://localhost:3000`
API сервер работает на: `http://localhost:5000`

## 🎯 Использование

1. **Откройте браузер** и перейдите на `http://localhost:3000`
2. **Выберите одну из трех функций** на главной странице
3. **Введите данные** (ключевое слово, тему, грамматику)
4. **Нажмите кнопку генерации** и дождитесь результата от ИИ
5. **Используйте материалы** для изучения английского!

## 📁 Структура проекта

```
project/
├── backend/
│   ├── models/
│   │   └── History.js          # Модель для хранения истории
│   ├── routes/
│   │   ├── essayRoutes.js      # Маршруты для генератора эссе
│   │   ├── dialogueRoutes.js   # Маршруты для диалогов
│   │   ├── fillBlankRoutes.js  # Маршруты для упражнений
│   │   └── historyRoutes.js    # Маршруты для истории
│   ├── services/
│   │   └── openaiService.js    # Сервис для работы с OpenAI
│   └── server.js               # Основной файл сервера
├── frontend/
│   ├── public/
│   │   └── index.html
│   └── src/
│       ├── components/
│       │   ├── Header.js & .css
│       │   ├── FeatureCard.js & .css
│       │   ├── EssayGenerator.js
│       │   ├── DialogueGenerator.js
│       │   ├── FillBlankGenerator.js
│       │   └── Generator.css
│       ├── App.js & .css
│       ├── index.js & .css
│       └── package.json
├── .env                        # Переменные окружения (создайте сами)
├── .env.example               # Пример переменных окружения
├── .gitignore
├── package.json
└── README.md
```

## 🔌 API Endpoints

### Генератор тем для эссе
```
POST /api/essay/generate
Body: { "keyword": "technology" }
Response: { "topics": [...] }
```

### Создатель диалогов
```
POST /api/dialogue/generate
Body: { "topic": "в ресторане", "level": "medium" }
Response: { "dialogue": {...} }
```

### Упражнения "Заполни пропуск"
```
POST /api/fill-blank/generate
Body: { "grammar": "Present Perfect", "count": 5 }
Response: { "exercises": [...] }
```

### История
```
GET /api/history?type=essay&limit=20
DELETE /api/history/:id
```

## 🎨 Дизайн

- **Современный градиентный фон** (фиолетово-синий)
- **Плавные анимации** появления элементов
- **Адаптивный дизайн** для мобильных устройств
- **Интуитивный интерфейс** с иконками
- **Цветовое кодирование** для разных функций

## 🚀 Возможности для расширения

- ✅ Добавить аутентификацию пользователей
- ✅ Сохранение прогресса обучения
- ✅ Статистика и аналитика
- ✅ Экспорт результатов в PDF
- ✅ Система достижений и мотивации
- ✅ Голосовое произношение (Text-to-Speech)
- ✅ Распознавание речи для диалогов

## 📝 Примечания

- **API лимиты**: OpenAI имеет лимиты на использование API. Убедитесь, что у вас есть средства на балансе.
- **MongoDB Atlas**: Бесплатный tier имеет ограничения по объему данных (512 МБ).
- **Производительность**: Генерация контента может занимать 3-10 секунд в зависимости от загрузки OpenAI.

## 🤝 Поддержка

Если у вас возникли проблемы:
1. Проверьте правильность API ключей в `.env`
2. Убедитесь, что MongoDB Atlas доступен
3. Проверьте консоль браузера и терминал на наличие ошибок
4. Убедитесь, что установлены все зависимости

## 📄 Лицензия

MIT License

---

**Создано с ❤️ для изучающих английский язык**

